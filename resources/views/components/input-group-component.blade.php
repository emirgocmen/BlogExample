<div class="input-group mb-3">
    <label for="title" class="w-100">{{ $label }}</label>
    <input type="text" class="form-control" value="{{ $value }}" placeholder="{{ $label }}" name="{{ $name }}" id="{{ $name }}">
</div>