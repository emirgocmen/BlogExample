<div class="form-floating mb-3">
    <label for="detail" class="w-100">{{ $label }}</label>
    <textarea class="form-control" name="{{ $name }}" id="{{ $name }}" style="height: 150px" placeholder="{{ $label }}">{{ $value }}</textarea>
</div>